# Public frontend configuration. Production must use HTTPS and a non-loopback host.
VITE_API_BASE_URL=https://api.example.com/api
# Comma-separated exact HTTPS origins. Empty means invoice redirects are disabled.
VITE_INVOICE_ORIGINS=